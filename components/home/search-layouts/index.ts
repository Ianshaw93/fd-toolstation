import CommandPaletteLayout from './CommandPaletteLayout';

/**
 * Swap this export to try another search-results prototype.
 * Matcher, catalogue, and deep links stay in `lib/tools/*`.
 * Homepage browse chrome (carousel) stays in ToolBrowser.
 *
 * Ian picked command palette. FilterGridLayout remains as an unused prototype.
 */
export { CommandPaletteLayout as ActiveSearchLayout };
